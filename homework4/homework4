#　x^2 - 3x + 1 = 0
#　暴力法

from numpy import arange
import math

def f(x):
    return  (x**2) - (3*x) +1

for x in arange(-100, 100, 0.001):
    if abs(f(x)) < 0.001:
        print("x=", x, " f(x)=", f(x))
        